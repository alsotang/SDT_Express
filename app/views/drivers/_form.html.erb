<%= form_for @driver, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :vehicle_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :vehicle_type, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :current_loc, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :current_loc, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :vehicle_status, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :vehicle_status, :class => 'check_box' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                drivers_path, :class => 'btn' %>
  </div>
<% end %>
